<h1>Tasks</h1>

<% counter = 1 %>
<h2><% @tasks.each do |task| %>
  <%= "#{counter}." %> <%=link_to(task[:title], show_path(task[:id]))  %>
  <% counter += 1 %>
  <br />
<% end %></h2>


<!-- link_to( post[:title], show_path(post[:id]) ) -->
